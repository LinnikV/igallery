<h1>Categories</h1>

<%= link_to "New", new_category_path, class:"btn btn-primary ", data: { turbo_frame: dom_id(Category.new) } %>

<%= turbo_frame_tag Category.new %>

<div id="new_category">
  <% @categories.each do |category| %>
    <%= link_to "#{category.title}", category %><br>
  <% end %>
</div>

