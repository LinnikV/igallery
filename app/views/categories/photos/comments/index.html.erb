<h3>Comments</h3>


<% @comments.each do |comment| %>
  <div class="card">
    <p>
      <strong>Сreated by:</strong>
      <%= comment.user.email %>
    </p>

    <p>
      <strong>Comment:</strong>
      <%= comment.body %>
    </p>
    
    <p>
      <strong>Category:</strong>
      <%= comment.photo.category.title %>
    </p>

    <p>
      <strong>Data:</strong>
      <%= comment.created_at.strftime("%H:%M  %d.%b.%Y")%><br>
    </p>
  </div>
<% end %>
